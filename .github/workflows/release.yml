name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get version from manifest
        id: version
        run: |
          BASE_VERSION=$(jq -r '.version' custom_components/pge_ebok/manifest.json)
          VERSION="${BASE_VERSION}.${{ github.run_number }}"
          echo "version=v$VERSION" >> "$GITHUB_OUTPUT"

          # Update the manifest with the exact version being released
          jq ".version = \"$VERSION\"" custom_components/pge_ebok/manifest.json > custom_components/pge_ebok/manifest.json.tmp
          mv custom_components/pge_ebok/manifest.json.tmp custom_components/pge_ebok/manifest.json

      - name: Create ZIP archive
        run: |
          cd custom_components/pge_ebok
          zip -r ../../pge_ebok.zip .
          cd ../..

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: Release ${{ steps.version.outputs.version }}
          files: pge_ebok.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
